testthat::context("parallel tempering")
testthat::test_that("parallel tempering runs without error", {
  # FIXME disabled for now -- segfault for unknown reason
  # gpds:::paralleltemperingTest1()
  # gpds:::paralleltemperingTest2()
})